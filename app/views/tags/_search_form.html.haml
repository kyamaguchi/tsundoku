.card
  .card-block
    %h4.card-title Search Tags
    = search_form_for q, url: tags_path do |f|
      - if q.name_eq.present?
        .row
          .col-sm-12
            = "Filtering with [#{q.name_eq}]"
            = link_to '(Clear)', tags_path
      .row
        .col-sm-12
          = check_box_tag :exclude_tagged, "1", params[:exclude_tagged] == '1'
          = f.label :exclude_tagged, 'Exclude tagged'
      .row
        .col-sm-12
          = f.label :name_cont, 'Name'
          = f.search_field :name_cont
      .row
        .col-sm-12
          = f.submit 'Search', class: 'btn btn-secondary'
